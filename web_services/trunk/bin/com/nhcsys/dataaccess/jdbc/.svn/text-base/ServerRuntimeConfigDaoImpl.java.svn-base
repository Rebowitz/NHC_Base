/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.nhcsys.dataaccess.jdbc;

import com.nhcsys.dataaccess.dao.*;
import com.nhcsys.dataaccess.factory.*;
import com.nhcsys.dataaccess.dto.*;
import com.nhcsys.dataaccess.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import org.apache.log4j.Logger;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class ServerRuntimeConfigDaoImpl extends AbstractDAO implements ServerRuntimeConfigDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	protected static final Logger logger = Logger.getLogger( ServerRuntimeConfigDaoImpl.class );

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT CONFIG_ID, CONFIG_KEY, CONFIG_VALUE FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	private int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( CONFIG_ID, CONFIG_KEY, CONFIG_VALUE ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET CONFIG_ID = ?, CONFIG_KEY = ?, CONFIG_VALUE = ? WHERE CONFIG_KEY = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE CONFIG_KEY = ?";

	/** 
	 * Index of column CONFIG_ID
	 */
	protected static final int COLUMN_CONFIG_ID = 1;

	/** 
	 * Index of column CONFIG_KEY
	 */
	protected static final int COLUMN_CONFIG_KEY = 2;

	/** 
	 * Index of column CONFIG_VALUE
	 */
	protected static final int COLUMN_CONFIG_VALUE = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column CONFIG_KEY
	 */
	protected static final int PK_COLUMN_CONFIG_KEY = 1;

	/** 
	 * Inserts a new row in the SERVER_RUNTIME_CONFIG table.
	 */
	public ServerRuntimeConfigPk insert(ServerRuntimeConfig dto) throws ServerRuntimeConfigDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( "SELECT config_id_seq.NEXTVAL FROM DUAL" );
			rs = stmt.executeQuery();
			if (rs.next()) {
				dto.setConfigId( rs.getLong( 1 ) );
				rs.close();
				stmt.close();
			}
		
			StringBuffer sql = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isConfigIdModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
				}
		
				sql.append( "CONFIG_ID" );
				modifiedCount++;
			}
		
			if (dto.isConfigKeyModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
				}
		
				sql.append( "CONFIG_KEY" );
				modifiedCount++;
			}
		
			if (dto.isConfigValueModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
				}
		
				sql.append( "CONFIG_VALUE" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			for (int i=0; i<modifiedCount; i++ ) {
				if (i>0) {
					sql.append( "," );
				}
		
				sql.append( "?" );
			}
		
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isConfigIdModified()) {
				stmt.setLong( index++, dto.getConfigId() );
			}
		
			if (dto.isConfigKeyModified()) {
				stmt.setString( index++, dto.getConfigKey() );
			}
		
			if (dto.isConfigValueModified()) {
				stmt.setString( index++, dto.getConfigValue() );
			}
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + sql.toString() + " with values: " + dto);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
			return dto.createPk();
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ServerRuntimeConfigDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the SERVER_RUNTIME_CONFIG table.
	 */
	public void update(ServerRuntimeConfigPk pk, ServerRuntimeConfig dto) throws ServerRuntimeConfigDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isConfigIdModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CONFIG_ID=?" );
				modified=true;
			}
		
			if (dto.isConfigKeyModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CONFIG_KEY=?" );
				modified=true;
			}
		
			if (dto.isConfigValueModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "CONFIG_VALUE=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE CONFIG_KEY=?" );
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + sql.toString() + " with values: " + dto);
			}
		
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isConfigIdModified()) {
				stmt.setLong( index++, dto.getConfigId() );
			}
		
			if (dto.isConfigKeyModified()) {
				stmt.setString( index++, dto.getConfigKey() );
			}
		
			if (dto.isConfigValueModified()) {
				stmt.setString( index++, dto.getConfigValue() );
			}
		
			stmt.setString( index++, pk.getConfigKey() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ServerRuntimeConfigDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the SERVER_RUNTIME_CONFIG table.
	 */
	public void delete(ServerRuntimeConfigPk pk) throws ServerRuntimeConfigDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL_DELETE + " with PK: " + pk);
			}
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setString( 1, pk.getConfigKey() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			if (logger.isDebugEnabled()) {
				logger.debug( rows + " rows affected (" + (t2-t1) + " ms)");
			}
		
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ServerRuntimeConfigDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the SERVER_RUNTIME_CONFIG table that matches the specified primary-key value.
	 */
	public ServerRuntimeConfig findByPrimaryKey(ServerRuntimeConfigPk pk) throws ServerRuntimeConfigDaoException
	{
		return findByPrimaryKey( pk.getConfigKey() );
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the criteria 'CONFIG_KEY = :configKey'.
	 */
	public ServerRuntimeConfig findByPrimaryKey(String configKey) throws ServerRuntimeConfigDaoException
	{
		ServerRuntimeConfig ret[] = findByDynamicSelect( SQL_SELECT + " WHERE CONFIG_KEY = ?", new Object[] { configKey } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the criteria ''.
	 */
	public ServerRuntimeConfig[] findAll() throws ServerRuntimeConfigDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY CONFIG_KEY", null );
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the criteria 'CONFIG_ID = :configId'.
	 */
	public ServerRuntimeConfig[] findWhereConfigIdEquals(long configId) throws ServerRuntimeConfigDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CONFIG_ID = ? ORDER BY CONFIG_ID", new Object[] {  new Long(configId) } );
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the criteria 'CONFIG_KEY = :configKey'.
	 */
	public ServerRuntimeConfig[] findWhereConfigKeyEquals(String configKey) throws ServerRuntimeConfigDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CONFIG_KEY = ? ORDER BY CONFIG_KEY", new Object[] { configKey } );
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the criteria 'CONFIG_VALUE = :configValue'.
	 */
	public ServerRuntimeConfig[] findWhereConfigValueEquals(String configValue) throws ServerRuntimeConfigDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CONFIG_VALUE = ? ORDER BY CONFIG_VALUE", new Object[] { configValue } );
	}

	/**
	 * Method 'ServerRuntimeConfigDaoImpl'
	 * 
	 */
	public ServerRuntimeConfigDaoImpl()
	{
	}

	/**
	 * Method 'ServerRuntimeConfigDaoImpl'
	 * 
	 * @param userConn
	 */
	public ServerRuntimeConfigDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "SERVER_RUNTIME_CONFIG";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ServerRuntimeConfig fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ServerRuntimeConfig dto = new ServerRuntimeConfig();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ServerRuntimeConfig[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ServerRuntimeConfig dto = new ServerRuntimeConfig();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ServerRuntimeConfig ret[] = new ServerRuntimeConfig[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ServerRuntimeConfig dto, ResultSet rs) throws SQLException
	{
		dto.setConfigId( rs.getLong( COLUMN_CONFIG_ID ) );
		dto.setConfigKey( rs.getString( COLUMN_CONFIG_KEY ) );
		dto.setConfigValue( rs.getString( COLUMN_CONFIG_VALUE ) );
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the specified arbitrary SQL statement
	 */
	public ServerRuntimeConfig[] findByDynamicSelect(String sql, Object[] sqlParams) throws ServerRuntimeConfigDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ServerRuntimeConfigDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the SERVER_RUNTIME_CONFIG table that match the specified arbitrary SQL statement
	 */
	public ServerRuntimeConfig[] findByDynamicWhere(String sql, Object[] sqlParams) throws ServerRuntimeConfigDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			if (logger.isDebugEnabled()) {
				logger.debug( "Executing " + SQL);
			}
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			logger.error( "Exception: " + _e.getMessage(), _e );
			throw new ServerRuntimeConfigDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
